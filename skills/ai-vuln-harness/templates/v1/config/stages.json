{
  "_comment": "Per-stage model pool and concurrency configuration. Model diversity between hunt and validate is load-bearing: it implements deliberate disagreement (different models are less likely to share the same blind spots). Trace runs on the validate pool because the evilsocket/audit paper calls it 'the stage that matters most'.",
  "defaults": {
    "repair_attempts": 1
  },
  "stages": {
    "recon": {
      "model_pool": "validate",
      "max_workers": 1,
      "repair_attempts": 2
    },
    "hunt": {
      "model_pool": "hunt",
      "max_workers": 10
    },
    "validate": {
      "model_pool": "validate",
      "max_workers": 5
    },
    "gapfill": {
      "model_pool": "hunt",
      "max_workers": 1
    },
    "dedupe": {
      "model_pool": "hunt",
      "max_workers": 1
    },
    "trace": {
      "model_pool": "validate",
      "max_workers": 5
    },
    "feedback": {
      "model_pool": "hunt",
      "max_workers": 1
    },
    "report": {
      "model_pool": "hunt",
      "max_workers": 1
    }
  }
}
